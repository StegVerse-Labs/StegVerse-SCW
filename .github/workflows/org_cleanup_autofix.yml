name: StegVerse Org Cleanup Auto-Fix Worker [CONTAINED — TV/TVC AUTHORITY REQUIRED]

on:
  workflow_dispatch: {}

permissions:
  contents: read

jobs:
  contained:
    runs-on: ubuntu-latest
    steps:
      - name: Block legacy credential-backed repository mutation
        shell: bash
        run: |
          set -euo pipefail
          echo "::error::This auto-fix worker is contained because the prior path combined generic PAT validation with contents write authority and direct commits/pushes."
          echo "Credential-free hygiene analysis may remain evidence-only; durable fixes require separately admitted TV/TVC repair authority with exact target/ref receipts. See SCW issue #22."
          exit 1
