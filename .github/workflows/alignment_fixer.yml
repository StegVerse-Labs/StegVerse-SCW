name: StegVerse Guardian Worker – Repo Alignment Auto-Fixer [CONTAINED]

on:
  workflow_dispatch: {}

permissions:
  contents: read

jobs:
  contained:
    runs-on: ubuntu-latest
    steps:
      - name: Block legacy repair authority
        shell: bash
        run: |
          set -euo pipefail
          echo "::error::Repo alignment auto-fixer is contained because its previous PAT-backed scheduled mutation path did not satisfy TV/TVC authority provenance."
          echo "Replacement must separate read-only source materialization from admitted repair mutation. See SCW issue #22."
          exit 1
