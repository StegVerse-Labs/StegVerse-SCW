name: StegVerse AI Entities (Phase 2) [CONTAINED]

on:
  workflow_dispatch: {}

permissions:
  contents: read

jobs:
  contained:
    runs-on: ubuntu-latest
    steps:
      - name: Block legacy entity mutation authority
        shell: bash
        run: |
          set -euo pipefail
          echo "::error::Scheduled/manual entity execution is contained because the prior PAT-backed job generated and directly pushed reports/memory under GitHub-hosted authority."
          echo "Entity execution and durable state mutation must be re-established through bounded TV/TVC and sovereign runtime authority. See SCW issue #22."
          exit 1
